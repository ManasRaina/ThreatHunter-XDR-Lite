# 🛡 ThreatIntel Platform

Automated Threat Intelligence Platform — Python · Flask · SQLite · Bootstrap 5

## Setup

```bash
cd tip
python -m venv venv && source venv/bin/activate   # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

Add your API keys to `.env` (URLHaus + ThreatFox work with no keys):